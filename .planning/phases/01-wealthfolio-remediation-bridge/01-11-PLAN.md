---
phase: 01-wealthfolio-remediation-bridge
plan: 11
type: execute
wave: 4
depends_on: [01-08, 01-09, 01-10]
files_modified:
  - tests/integration/test_phase01_wealthfolio_lifecycle.py
  - tests/integration/test_phase01_wealthfolio_migration.py
autonomous: true
gap_closure: true
requirements: []
user_setup:
  - service: postgres-and-redis
    why: "Database-backed cursor, backlog, and executor lifecycle tests require the repository integration services."
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Repository test environment / docker-compose.test.yml PostgreSQL"
      - name: TEST_REDIS_URL
        source: "Repository test environment / docker-compose.test.yml Redis"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A successful health poll persists one backlog row per target/entity and a repeated poll updates the existing row."
    - "Failed remote verification leaves an item retry_wait (or manual_review after the configured limit), and only confirmed remote health permits resolved."
    - "Cursor migration and target-scoped state are validated against the database when dependencies are available; otherwise the result is explicitly skipped/unverified."
    - "Control-plane and deployment checks remain manual checkpoints rather than being falsely marked automated."
  artifacts:
    - "Database-backed lifecycle regression covering enqueue_success, disappearance guard, executor verification transition, and target isolation."
    - "Migration test asserts cursor uniqueness, tenant/target isolation, and complete/truncated/cursor-state columns."
  key_links:
    - "worker poll -> database cursor/backlog -> remediation claim -> strategy verify -> status transition"
    - "integration dependency availability -> explicit pytest skip/report, never a silent pass"
---

<objective>
Close the two behavior-unverified lifecycle gaps and convert the unavailable database validation into an explicit, repeatable integration gate.

Purpose: Prove the durable state machine behind the bridge, not only normalization and isolated helper behavior.
Output: Database/lifecycle regression coverage and honest environment reporting.
</objective>

<execution_context>
@/Users/ruben/.codex/gsd-core/workflows/execute-plan.md
@/Users/ruben/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-CONTEXT.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-RESEARCH.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-VERIFICATION.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-VALIDATION.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-SUMMARY.md
@src/finance_sync/reconciliation/remediation/executor.py
@tests/integration/test_phase01_wealthfolio_migration.py
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace the persisted poll-to-resolution lifecycle with remote verification outcomes</name>
  <files>tests/integration/test_phase01_wealthfolio_lifecycle.py</files>
  <action>Create a named PostgreSQL-backed lifecycle regression in `tests/integration/test_phase01_wealthfolio_lifecycle.py` using the repository's async session and integration fixtures. The test must persist a target-scoped finding, repeat the poll and observe one durable backlog row, claim and execute it with mocked Wealthfolio HTTP responses only, assert failed remote verification yields `retry_wait`, then assert only a subsequent positive verification yields `resolved`. Include two targets in one tenant and an incomplete-poll case so the persisted test covers D-01, D-03, D-05, D-06, D-08, and executor ordering. Deterministic fakes may stand in for remote HTTP responses, but they must never replace the database session or satisfy a persisted-lifecycle claim. If PostgreSQL/Redis or required integration dependencies are absent, the test must emit an explicit skip reason and the executor must record the result as `SKIPPED/UNVERIFIED`, never fabricate a green result. Commit atomically as test(01-11): cover persisted Wealthfolio remediation lifecycle.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest -m integration tests/integration/test_phase01_wealthfolio_lifecycle.py -q</automated><fails_when>The lifecycle test does not observe persisted idempotency, failed verification resolves an item, a capped/failed poll resolves work, target routing is not asserted, a fake substitutes for database persistence, or the integration command fails when dependencies are available.</fails_when><environment>If uv or project dependencies are unavailable, or PostgreSQL/Redis is unavailable, retain the test's explicit skip reason and record `SKIPPED/UNVERIFIED` with the missing dependency in the phase summary; do not treat the skip as a passing lifecycle proof.</environment></verify>
  <done>A real database-backed lifecycle test proves durable idempotency, target isolation, incomplete-poll protection, verify-before-resolve ordering, and explicit unavailable-environment semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Execute or explicitly skip database migration validation</name>
  <files>tests/integration/test_phase01_wealthfolio_migration.py</files>
  <action>Complete the migration contract for empty and existing databases, including cursor uniqueness by tenant and target and the `complete`, `truncated`, and bounded `cursor_state` columns owned by 01-09. Assert the migration chain applies cleanly and the persisted schema exposes the required indexes/constraints. Run it against the repository Docker PostgreSQL/Redis services when TEST_DATABASE_URL and TEST_REDIS_URL are available. Preserve the test's explicit skip reason when dependencies are unavailable and record the command/result in the phase summary as `SKIPPED/UNVERIFIED`; never convert a missing service into a passing assertion per D-16. This task owns only migration integration assertions; 01-08 owns the separate legacy backfill migration and regression. Commit atomically as test(01-11): validate Wealthfolio cursor migration.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest -m integration tests/integration/test_phase01_wealthfolio_migration.py -q</automated><fails_when>The migration test fails when services are available, silently passes without a database, omits tenant/target uniqueness or completeness columns, or reports an unavailable dependency without an explicit skipped/unverified result.</fails_when><environment>If uv/project dependencies or PostgreSQL/Redis are unavailable, preserve the explicit skip reason, record `SKIPPED/UNVERIFIED` and the missing dependency in the phase summary, and retain the manual verification checkpoint.</environment></verify>
  <done>Migration/state validation is green with configured services or visibly skipped with the exact missing dependency and retained manual checkpoint.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Database state -> executor | Persisted identities and statuses control credential-bearing remediation. |
| Test environment -> validation result | Missing external dependencies must not be misrepresented as verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-11-01 | Tampering | backlog lifecycle transitions | high | mitigate | Assert verify-before-resolve and complete-snapshot guards in a named lifecycle test. |
| T-01-11-02 | Elevation of privilege | target-scoped persistence | high | mitigate | Assert tenant and target uniqueness plus exact connector routing. |
| T-01-11-03 | Repudiation | skipped integration dependency | medium | mitigate | Emit explicit pytest skip/unverified status and preserve manual verification per D-16. |
</threat_model>

<verification>
Run the lifecycle unit/integration command, then the migration command. If PostgreSQL or Redis is unavailable, retain the explicit skip and report it as unverified in the summary; do not claim full phase verification.
</verification>

<success_criteria>
Both behavior-unverified items from 01-VERIFICATION.md have named automated coverage, and migration availability is represented honestly in validation output.
</success_criteria>

<output>
Create `.planning/phases/01-wealthfolio-remediation-bridge/01-11-SUMMARY.md` when done.
</output>
