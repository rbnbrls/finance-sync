---
phase: 01-wealthfolio-remediation-bridge
plan: 10
type: execute
wave: 3
depends_on: [01-08, 01-09]
files_modified:
  - src/finance_sync/services/wealthfolio_health_bridge.py
  - src/finance_sync/reconciliation/remediation/wealthfolio.py
  - src/finance_sync/reconciliation/remediation/price_history.py
  - src/finance_sync/reconciliation/remediation/quote.py
  - tests/test_wealthfolio_remediation_contract.py
autonomous: true
gap_closure: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Safe quote and historical-price findings resolve canonical security identity before automatic repair."
    - "ISIN, ticker, and provider-specific identifiers preserve identifier_type through enrichment and projection."
    - "Unresolved or ambiguous identity remains manual_review and does not mutate canonical or remote data."
    - "Historical projection and verification use the same half-open [start,end) interval, with date-only end normalized to the next day."
  artifacts:
    - "Bridge uses the existing canonical security lookup and persists resolved security_id, identifier, and identifier_type in bounded context."
    - "Wealthfolio historical strategy applies identical interval semantics in execute and verify."
    - "Regression tests cover asset-only resolution, identifier types, unresolved identity, and end-date boundary behavior."
  key_links:
    - "health affected item -> canonical lookup -> remediation context -> EnrichmentGateway/PriceStore -> Wealthfolio upsert"
    - "historical context window -> local projection query and verification query"
---

<objective>
Close WR-01 and WR-02 while preserving the locked safe-repair boundary and exact target routing established by 01-08/01-09.

Purpose: Make advertised quote/history repair work for real Wealthfolio asset payloads without guessing identity or producing false verification failures.
Output: Canonical identity-aware, interval-consistent repair strategies with contract tests.
</objective>

<execution_context>
@/Users/ruben/.codex/gsd-core/workflows/execute-plan.md
@/Users/ruben/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-CONTEXT.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-RESEARCH.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-VERIFICATION.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-REVIEW.md
@src/finance_sync/enrichment/gateway.py
@src/finance_sync/enrichment/security_resolver.py
@src/finance_sync/reconciliation/remediation/quote.py
@src/finance_sync/reconciliation/remediation/price_history.py
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace one asset-only health finding through canonical lookup and remote quote projection</name>
  <files>src/finance_sync/services/wealthfolio_health_bridge.py, src/finance_sync/reconciliation/remediation/wealthfolio.py, src/finance_sync/reconciliation/remediation/quote.py, tests/test_wealthfolio_remediation_contract.py</files>
  <action>Use the repository's existing EnrichmentGateway/security resolver lookup rather than a new identity table or provider-specific matcher. Normalize asset-only and identifier-bearing affectedItems into a proven security_id plus identifier and identifier_type, preserving explicit ISIN, ticker, or provider symbol type per D-09 and D-10. If lookup is missing or ambiguous, set manual_review context and keep the unsupported/manual strategy. Wire one proven quote path through canonical quote acquisition, exact Wealthfolio asset upsert, and remote verification; do not mutate purchase, valuation, transaction, or transfer data per D-11. Commit atomically as fix(01-10): resolve canonical Wealthfolio asset identity.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_remediation_contract.py -q -k 'identity or asset or quote or identifier'</automated><fails_when>An asset-only finding cannot resolve through the existing lookup, identifier_type is lost or changed, unresolved identity receives an automatic strategy, or the tracer contract test fails.</fails_when><environment>If uv or project dependencies are unavailable, record `SKIPPED/UNVERIFIED` and the exact missing prerequisite in the phase summary, retain the manual verification checkpoint, and do not report the contract as passed.</environment></verify>
  <done>A common asset-only quote finding reaches canonical data and targeted remote projection with proven identity; unresolved identities remain manual_review.</done>
</task>

<task type="auto">
  <name>Task 2: Align historical projection and verification to half-open date windows</name>
  <files>src/finance_sync/reconciliation/remediation/wealthfolio.py, src/finance_sync/reconciliation/remediation/price_history.py, tests/test_wealthfolio_remediation_contract.py</files>
  <action>Normalize date-only end values to the next day and apply [start,end) consistently to historical projection, batching, canonical enrichment calls, and verification per D-12. Preserve timezone-aware timestamps and reject invalid/reversed windows without provider writes. Add boundary tests proving an observation exactly at end is excluded and an observation immediately before end is included, plus a date-only end test that avoids false retry/manual_review after successful projection. Commit atomically as fix(01-10): enforce half-open Wealthfolio history windows.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_remediation_contract.py -q -k 'historical or interval or window or end_date'</automated><fails_when>Projection and verification disagree on the end boundary, date-only end values remain midnight-inclusive, invalid windows trigger remote writes, or the interval test command fails.</fails_when><environment>If uv or project dependencies are unavailable, record `SKIPPED/UNVERIFIED` with the exact missing prerequisite in the phase summary and retain the manual verification checkpoint; an unavailable command is not a pass.</environment></verify>
  <done>Historical repair and verification share a documented half-open interval and boundary regressions pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Wealthfolio identifiers -> canonical lookup | Untrusted remote IDs must not select an unrelated security. |
| Canonical price store -> Wealthfolio writer | Only verified quote/history data may cross into the target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-10-01 | Elevation of privilege | canonical identity resolution | high | mitigate | Require unambiguous existing lookup result and manual_review otherwise per D-09. |
| T-01-10-02 | Tampering | historical interval handling | medium | mitigate | Normalize end dates once and use half-open predicates in projection and verification per D-12. |
| T-01-10-03 | Tampering | unsafe remediation strategies | high | mitigate | Keep non-quote/history categories unsupported/manual_review per D-11. |
</threat_model>

<verification>
Run both focused contract commands, then `APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_remediation_contract.py tests/test_phase01_wealthfolio_bridge.py -q`. If uv or dependencies are unavailable, record the exact `SKIPPED/UNVERIFIED` result and missing prerequisite in the summary and keep the phase manual checkpoint; preserve each task's fails_when directions.
</verification>

<success_criteria>
WR-01 and WR-02 are closed, canonical identity and identifier-type paths are regression-tested, and safe repairs remain limited to quote/history data.
</success_criteria>

<output>
Create `.planning/phases/01-wealthfolio-remediation-bridge/01-10-SUMMARY.md` when done.
</output>
