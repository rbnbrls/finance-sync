---
phase: 01-wealthfolio-remediation-bridge
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finance_sync/services/wealthfolio_health_bridge.py
  - src/finance_sync/reconciliation/remediation/backlog.py
  - src/finance_sync/worker/jobs.py
  - migrations/versions/0072_backfill_wealthfolio_remediation_target_identity.py
  - tests/test_wealthfolio_health_bridge.py
  - tests/integration/test_phase01_legacy_remediation_backfill.py
autonomous: true
gap_closure: true
requirements: []
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Two active Wealthfolio targets in one tenant produce independently deduplicated backlog items and route repairs to the exact recorded target."
    - "Purchase/cost-basis, valuation, transaction/transfer, and unknown findings are manual_review-only and do not select automatic quote/history strategies."
    - "Legacy items with an unambiguous target are preserved and target-scoped; ambiguous legacy items are manual_review without guessing."
  artifacts:
    - "Target identity is carried in DetectedIssue.connection_id/scope and the persisted backlog key."
    - "Normalization tests cover unsafe category precedence, two-target identity, and legacy ambiguity."
  key_links:
    - "normalize_health_issues -> DetectedIssue -> deduplication_key -> BacklogRepository.register"
    - "backlog connection_id/context target_id -> remediation connector factory exact ExportTarget lookup"
---

<objective>
Close CR-01 and CR-02 and the legacy-identity portion of D-04 by making Wealthfolio target identity part of the shared backlog contract and enforcing fail-closed unsafe classification.

Purpose: Prevent automatic financial mutation for unsafe findings and prevent two targets from overwriting each other's remediation lifecycle.
Output: Target-safe normalization/backlog wiring with regression coverage.
</objective>

<execution_context>
@/Users/ruben/.codex/gsd-core/workflows/execute-plan.md
@/Users/ruben/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-CONTEXT.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-RESEARCH.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-VERIFICATION.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-REVIEW.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-SUMMARY.md
@src/finance_sync/services/wealthfolio_health_bridge.py
@src/finance_sync/reconciliation/remediation/backlog.py
@src/finance_sync/worker/jobs.py
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace one Wealthfolio finding through target-scoped backlog registration</name>
  <files>src/finance_sync/services/wealthfolio_health_bridge.py, src/finance_sync/reconciliation/remediation/backlog.py, src/finance_sync/worker/jobs.py, tests/test_wealthfolio_health_bridge.py</files>
  <action>Wire one real health finding from normalize_health_issues into DetectedIssue and BacklogRepository.register with the existing ExportTarget database ID as the stable target identity per D-01 and D-02. Set the persisted connection_id/scope used by deduplication, retain target_id in sanitized context, and make the remediation connector factory consume that exact target identity per D-03. Preserve existing legacy rows where their target_id is uniquely resolvable; transition rows with no unique target match to manual_review with bounded context per D-04. Add a regression that registers the same issue for two targets in one tenant and asserts separate identities and exact routing. Commit atomically as fix(01-08): scope Wealthfolio backlog identity.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_health_bridge.py -q</automated><fails_when>Any two-target regression collapses to one deduplication key, connector routing uses a different target than the item identity, or the focused bridge test command is not green.</fails_when><environment>If uv or the project dependencies are unavailable, record `SKIPPED/UNVERIFIED` with the missing executable/dependency in the phase summary and retain the manual verification checkpoint; do not report the task as passed.</environment></verify>
  <done>Target identity participates in the persisted deduplication contract, two same-tenant targets remain isolated, and the exact target is used for remediation connector construction.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce unsafe category precedence and legacy fail-closed routing</name>
  <files>src/finance_sync/services/wealthfolio_health_bridge.py, migrations/versions/0072_backfill_wealthfolio_remediation_target_identity.py, tests/test_wealthfolio_health_bridge.py, tests/integration/test_phase01_legacy_remediation_backfill.py</files>
  <action>Update _issue_kind and strategy selection so purchase/cost-basis, negative/incomplete valuation, transaction/transfer, and unknown categories are classified before generic price matching and remain manual_review-only per D-11. Add executable migration `migrations/versions/0072_backfill_wealthfolio_remediation_target_identity.py` that reads only bounded legacy `context.target_id` values, resolves a unique tenant-matching ExportTarget into `connection_id`, and transitions records with no target or more than one valid candidate to `manual_review` with a bounded reason; it must never guess from endpoint/account fields. Add a PostgreSQL-backed regression in `tests/integration/test_phase01_legacy_remediation_backfill.py` covering unique, ambiguous, and unresolved legacy rows, plus unit cases for MISSING_PURCHASE_PRICE, valuation variants, transaction/transfer, and unknown codes asserting no automatic strategy or canonical mutation path. Commit atomically as fix(01-08): fail closed unsafe Wealthfolio findings and backfill legacy target identity.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_phase01_wealthfolio_bridge.py -q -k 'health_findings or unsafe or purchase or valuation or unknown'</automated><fails_when>The purchase-price regression still selects a quote/history strategy, any unsafe category lacks manual_review routing, the backfill migration lacks database assertions for unique/ambiguous/unresolved rows, or the focused classification command fails.</fails_when><environment>If uv/project dependencies are unavailable, record `SKIPPED/UNVERIFIED` and the missing dependency in the phase summary. The PostgreSQL backfill regression must use its explicit integration skip reason when the database is unavailable; a skipped integration test is unverified, not a pass.</environment></verify>
  <done>All unsafe and unknown categories remain manual_review-only with regression coverage, while safe quote/history categories retain their automatic strategy mapping.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Wealthfolio response -> normalization | Untrusted remote categories and identifiers enter backlog state. |
| Backlog item -> remediation connector | Persisted target identity selects a credential-bearing destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08-01 | Elevation of privilege | target lookup in jobs.py | high | mitigate | Require tenant-scoped exact ExportTarget lookup from persisted target identity per D-03. |
| T-01-08-02 | Tampering | issue classification | high | mitigate | Unsafe-category precedence and manual_review-only strategy mapping per D-11. |
| T-01-08-03 | Information disclosure | backlog context | medium | mitigate | Retain only bounded identifiers/reasons; never copy credentials or raw payloads. |
</threat_model>

<verification>
Run the two focused pytest commands in task order, then run `APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_health_bridge.py tests/test_phase01_wealthfolio_bridge.py -q`.
</verification>

<success_criteria>
CR-01 and CR-02 are closed, D-01 through D-04 and D-11 are exercised by regression tests, and no unsafe finding can reach an automatic strategy.
</success_criteria>

<output>
Create `.planning/phases/01-wealthfolio-remediation-bridge/01-08-SUMMARY.md` when done.
</output>
