---
phase: 01-wealthfolio-remediation-bridge
plan: 09
type: execute
wave: 2
depends_on: [01-08]
files_modified:
  - src/finance_sync/services/wealthfolio_health_bridge.py
  - src/finance_sync/worker/jobs.py
  - src/finance_sync/models/wealthfolio_health_cursor.py
  - src/finance_sync/exporter/wealthfolio/client.py
  - src/finance_sync/services/data_health.py
  - tests/test_phase01_wealthfolio_bridge.py
  - tests/test_wealthfolio_health_bridge.py
autonomous: true
gap_closure: true
requirements: []
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Capped or otherwise incomplete health snapshots persist incomplete state and never resolve omitted active items."
    - "Failed polls leave active remediation items unchanged and remain retryable."
    - "Transient httpx.RequestError failures receive bounded backoff, while authentication/configuration failures fail fast."
    - "Data Health enabled reflects the bridge feature flag separately from target availability and degraded target state."
  artifacts:
    - "Cursor state records complete/truncated semantics, cursor/progress metadata, and observable metrics."
    - "Client, worker, and Data Health regression tests cover transport retries, cap handling, and disabled-state reporting."
  key_links:
    - "health response -> completeness metadata -> cursor persistence/metrics -> guarded disappearance reconciliation"
    - "Settings.wealthfolio_health_bridge_enabled -> DataHealthWealthfolioBridge.enabled"
---

<objective>
Close CR-03, WR-03, and WR-04 by making health snapshot completeness durable, protecting lifecycle reconciliation, retrying transient transport errors, and reporting operational truth.

Purpose: Ensure missing remote data is never mistaken for resolved data and operators can distinguish feature enablement from target health.
Output: Durable completeness-aware polling and truthful operational status.
</objective>

<execution_context>
@/Users/ruben/.codex/gsd-core/workflows/execute-plan.md
@/Users/ruben/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-CONTEXT.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-RESEARCH.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-VERIFICATION.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-REVIEW.md
@.planning/phases/01-wealthfolio-remediation-bridge/01-08-PLAN.md
</context>

<tasks>
<task type="tracer">
  <name>Task 1: Trace capped and failed polls through durable lifecycle guards</name>
  <files>src/finance_sync/services/wealthfolio_health_bridge.py, src/finance_sync/worker/jobs.py, src/finance_sync/models/wealthfolio_health_cursor.py, tests/test_phase01_wealthfolio_bridge.py, tests/test_wealthfolio_health_bridge.py</files>
  <action>Replace the worker's lossy list slicing with an explicit poll result/completeness contract per D-05, D-06, D-07, and D-08. Prefer bounded pagination or a complete server-side fetch when the Wealthfolio response supports it; otherwise persist complete=false/truncated=true, cursor state, and bounded metrics and skip disappearance reconciliation. A successful complete empty snapshot may reconcile; a capped, malformed, partial, or failed poll must not. Keep previously active items unchanged on failure. Add tests for over-limit snapshots, complete empty snapshots, and failed polls. Commit atomically as fix(01-09): guard incomplete Wealthfolio reconciliation.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_health_bridge.py tests/test_phase01_wealthfolio_bridge.py -q -k 'cap or truncat or complete or failure or disappearance or health_poll'</automated><fails_when>An omitted issue is resolved from a capped/partial response, a failed poll changes active item status, completeness is not persisted/observable, or the focused lifecycle command fails.</fails_when></verify>
  <done>Only complete successful snapshots can reconcile disappearance; capped and failed polls preserve active work and expose durable incomplete/error state.</done>
</task>

<task type="auto">
  <name>Task 2: Complete transport retry and feature-flag operational reporting</name>
  <files>src/finance_sync/exporter/wealthfolio/client.py, src/finance_sync/services/data_health.py, tests/test_phase01_wealthfolio_bridge.py, tests/exporter/test_wealthfolio_client.py</files>
  <action>Extend get_health_status to retry transient httpx.RequestError failures with the existing bounded exponential backoff per D-13, while preserving immediate auth/configuration failure behavior and sanitized diagnostics. Inject the existing Settings flag into DataHealthService and report enabled from that flag; keep target_count, last success, error, and incomplete/degraded signals separate per D-14 and D-15. Add regressions for connection reset/DNS-style RequestError retry and active-target-plus-disabled-flag reporting. Commit atomically as fix(01-09): report truthful bridge health.</action>
  <verify><automated>APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_phase01_wealthfolio_bridge.py tests/exporter/test_wealthfolio_client.py -q -k 'health_poll or request_error or transport or enabled or bridge'</automated><fails_when>Generic transient RequestError is raised without bounded retry, credentials/raw response data leak into the error, or Data Health reports enabled solely because a target exists.</fails_when></verify>
  <done>Transport outages retry within the configured bound, auth/config errors fail fast, and Data Health separates feature enablement from target/degraded status.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Wealthfolio HTTP response -> poll state | Remote availability and response completeness control backlog transitions. |
| Settings -> Data Health API | Operational configuration is projected to tenant-facing status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-09-01 | Tampering | disappearance reconciliation | high | mitigate | Require durable complete=true before resolving omitted findings per D-05/D-08. |
| T-01-09-02 | Denial of service | health client retry loop | medium | mitigate | Bound attempts and exponential delay; do not retry auth/configuration errors. |
| T-01-09-03 | Information disclosure | poll errors/metrics | medium | mitigate | Keep messages sanitized and metrics credential-free. |
</threat_model>

<verification>
Run both focused commands, then `APP_ENVIRONMENT=dev DEBUG=false uv run pytest tests/test_wealthfolio_health_bridge.py tests/test_phase01_wealthfolio_bridge.py tests/exporter/test_wealthfolio_client.py -q`.
</verification>

<success_criteria>
CR-03, WR-03, and WR-04 are closed, D-05 through D-08 and D-13 through D-16 are represented in tests or explicitly marked environment-skipped.
</success_criteria>

<output>
Create `.planning/phases/01-wealthfolio-remediation-bridge/01-09-SUMMARY.md` when done.
</output>
